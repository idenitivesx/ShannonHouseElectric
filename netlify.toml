# DEPRECATED — migrated to Vercel (iiden5796). See vercel.json.
# Kept for reference only; disconnect this site from GitHub in Netlify if still linked.

[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

[[headers]]
  for = "/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"
